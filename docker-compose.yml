version: '3.8'

services:
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    container_name: oml-aggr-mcp-db
    environment:
      POSTGRES_USER: aggr
      POSTGRES_PASSWORD: aggr_secret
      POSTGRES_DB: aggr
    volumes:
      - /volume1/docker/oml-aggr-mcp-db:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    mem_limit: 1536m
    cpus: 1.5
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aggr -d aggr"]
      interval: 10s
      timeout: 5s
      retries: 5

  mcp-hub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oml-aggr-mcp
    environment:
      DB_URL: postgres://aggr:aggr_secret@timescaledb:5432/aggr?sslmode=disable
      SYMBOLS: BTCUSDT
      LOG_LEVEL: info
    ports:
      - "8080:8080"
      - "8081:8081"
    mem_limit: 1024m
    cpus: 1.5
    restart: unless-stopped
    depends_on:
      timescaledb:
        condition: service_healthy
    volumes:
      - /volume1/docker/oml-aggr-mcp-logs:/app/logs
